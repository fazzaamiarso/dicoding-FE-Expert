import { css } from "lit";

// eslint-disable-next-line import/prefer-default-export
export const utilClasses = css`
  .layout {
    width: 90%;
    margin-inline: auto;
    max-width: 1200px;
  }

  .click-area {
    min-height: 44px;
    min-width: 44px;
  }
`;
